import { JSONResponse, ImageResponse, f, AnimalEndpoints, ImageEndpoints, Options } from "../typings";
export default class Animals implements ImageEndpoints<AnimalEndpoints> {
    options: Options;
    constructor(d?: Partial<Options>);
    sendRequest(cat: AnimalEndpoints, method: "image"): Promise<ImageResponse>;
    sendRequest(cat: AnimalEndpoints, method: "json", amount: 1): Promise<JSONResponse>;
    sendRequest(cat: AnimalEndpoints, method: "json", amount?: 2 | 3 | 4 | 5): Promise<JSONResponse[]>;
    readonly birb: typeof f;
    readonly blep: typeof f;
    readonly cheeta: typeof f;
    readonly fox: typeof f;
    readonly lynx: typeof f;
    readonly wolf: typeof f;
    private readonly debug;
}
