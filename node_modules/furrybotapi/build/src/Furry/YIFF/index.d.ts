import { JSONResponse, ImageResponse, f, YiffEndpoints, ImageEndpoints, Options } from "../../typings";
export default class YIFF implements ImageEndpoints<YiffEndpoints> {
    options: Options;
    constructor(d?: Partial<Options>);
    sendRequest(cat: YiffEndpoints, method: "image"): Promise<ImageResponse>;
    sendRequest(cat: YiffEndpoints, method: "json", amount: 1): Promise<JSONResponse>;
    sendRequest(cat: YiffEndpoints, method: "json", amount?: 2 | 3 | 4 | 5): Promise<JSONResponse[]>;
    readonly dickgirl: typeof f;
    readonly gay: typeof f;
    readonly lesbian: typeof f;
    readonly straight: typeof f;
    private readonly debug;
}
